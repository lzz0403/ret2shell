[[images]]
name = "image1"
tag = "image1:latest"
cpu = 0.1
mem = "128Mi"
# Expose port 80 for the player, only the first image port will be exposed,
# subsequent images will be ignored
port = 80

[[images]]
name = "image2"
tag = "image2:latest"
cpu = 0.1
mem = "128Mi"
# No port exposed
